[project]
name = "gmail-automation"
version = "0.1.0"
description = "Local Gmail inbox cleanup helper."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.115.12",
    "google-api-python-client==2.170.0",
    "google-auth-httplib2==0.2.0",
    "google-auth-oauthlib==1.2.2",
    "PyYAML==6.0.2",
    "truststore==0.10.1",
    "uvicorn[standard]==0.34.3",
]

[tool.uv]
package = false
